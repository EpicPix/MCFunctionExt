on: [push, pull_request]

name: CI

jobs:
  compile:
    runs-on: ubuntu-latest
    name: Compile
    steps:
      - name: Clone repository
        uses: actions/checkout@v2

      - name: Compile
        run: make